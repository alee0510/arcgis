/**
 * @type {import('@react-native-community/cli-types').UserDependencyConfig}
 */
module.exports = {
  dependency: {
    platforms: {
      android: {
        // Auto-linked TurboModules use default codegen paths
      },
    },
  },
};
