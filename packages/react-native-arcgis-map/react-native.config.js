/**
 * @type {import('@react-native-community/cli-types').UserDependencyConfig}
 */
module.exports = {
  dependency: {
    platforms: {
      android: {
        // Auto-linked Fabric components use default codegen paths
      },
    },
  },
};
